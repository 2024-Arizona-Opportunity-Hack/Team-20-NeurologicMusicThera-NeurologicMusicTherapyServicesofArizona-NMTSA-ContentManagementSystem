import SparklesText from "../ui/sparkles-text";

export function Hero() {
    return (
        <section id="home" className="mt-3 p-10 rounded-lg mx-auto flex flex-col items-center border-solid border-black">
        <h1 className="font-semibold text-center relative w-[max-content] text-5xl">
      Unleashing the <span className="font-extrabold text-primary">unqiue</span> potential of the disabled.
        </h1>
      </section>
    );
}